import 'package:flutter/material.dart';

class ValidityOverScreen extends StatelessWidget {
  const ValidityOverScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
