import 'package:flutter/material.dart';

class TaskTypeAllScreen extends StatelessWidget {
  const TaskTypeAllScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Task Type"),
      ),
    );
  }
}
